package com.revature.repository;

public interface PhoneBook {
	
	/*
	 * Placeholder interface for keeping track of Customers. 
	 * 
	 * In case we want to expand the functionality of our Cafe to keep track of our customer's order, 
	 * 	we'll need a Customer object in our models layer (with MenuItems). 
	 * 
	 * We would have methods such as: 
	 * 
	 * List<Customer> allCustomers();
	 * 
	 * Customer customerByName(String name);
	 * 
	 * boolean deleteCustomer(String name);
	 * 
	 * boolean updateCustomerDetails(Customer c);
	 * 
	 */

}
